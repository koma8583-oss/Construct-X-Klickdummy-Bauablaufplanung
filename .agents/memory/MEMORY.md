- [Express v5 params typing](express-v5-params.md) — ParamsDictionary in @types/express-serve-static-core@5.1.2 is `string | string[]`; add `as string` casts to all req.params access in drizzle eq() calls.
- [DB schema & backend routes](taktkoord-backend.md) — TaktKoord backend: JWT auth (bcryptjs + jsonwebtoken), 10 schema files (added takt-requests, takt-responses), full route set; zod v4 in schema files, plain zod in route files.
- [JWT Auth Service](jwt-auth-service.md) — centralized auth at /auth-service; Express.Request global namespace is the correct req.user extension point; lib/db needs tsc rebuild after schema changes.
- [DB push via psql not drizzle-kit](db-push-psql.md) — drizzle-kit push hangs on TTY prompts in non-interactive shells; use raw psql SQL with IF NOT EXISTS + idempotent DO $$ BEGIN … EXCEPTION WHEN duplicate_object THEN NULL; END $$ for enum creation.
- [TaktKoord Sprint 2 model](sprint2-takt-model.md) — new tables: takt_requests, takt_request_snapshots, takt_responses, takt_response_alternatives + takte.version/lifecycle_status; state machine in takt-request-transitions.ts; Vitest installed in api-server.
- [Drizzle unique constraint error](drizzle-unique-error.md) — drizzle wraps pg errors; check `(err as {cause?: {code?: string}}).cause?.code === "23505"`, not top-level `err.code`, for unique constraint violations.
- [NU resource planning model](nu-resource-planning.md) — availability_checks + nu_local_projects + resource_bookings tables added; runAvailabilityCheck service in api-server/src/services/; alternative generator in alternative-generator.ts; all /nu/* routes AN-only guarded.
- [Sprint 4 test fixture pitfalls](sprint4-test-fixtures.md) — takte requires planned_start/planned_end (NOT NULL); takt_requests requires created_by_user_id FK; E2E needs project_contractors entry for createTaktRequestWithSnapshot; use string dates not new Date() for mode:"string" columns.
- [Response retry idempotency](response-retry-idempotency.md) — retry path must NOT re-call transport.send(); query existing outbox row by messageId instead to avoid payload mismatch (date format string vs ISO datetime) causing InvalidEnvelopeError → 500.
- [Inbox org-neutral](inbox-org-neutral.md) — requireNuOrg renamed to requireOrg accepting AG+AN; hub-admin (orgId:null) still blocked; GU accessing NU messages → 404 (not 403) for GET/:id and markAsRead → 403 (RecipientForbiddenError).
- [Test teardown FK order](test-teardown-fk-order.md) — strict deletion order required across all test afterAll/beforeAll blocks; message_outbox must be flushed before organizations; takt_versions before takt_response_decisions; never do global (unfiltered) table deletes.
- [Missing /api/ prefix in tests](test-api-prefix.md) — all routes are mounted at /api/...; test files that call request(app).post('/takt-requests/...') without /api/ get 404; always use /api/takt-requests/... in supertest calls.
- [Response API field names](response-api-fields.md) — NU response body uses `acceptedTimeWindow:{start,end}` not `acceptedStart/End`; alternatives need `{alternativeId, rank, timeWindow:{start,end}}` not `proposedStart/End`; GU decision `acceptedAlternativeId` is the row UUID (`takt_response_alternatives.id`), not the business `alternativeId` string.
- [Test setup: direct DB not API](test-db-inserts.md) — always insert test fixtures directly (established pattern); API-based setup is fragile; use `jwt.sign()` for tokens, never API login; response endpoint rejects DELIVERED status for new responses (use UNDER_REVIEW).
- [Deadline worker architecture](deadline-worker.md) — evaluateTaktRequestDeadlines() in deadline-evaluation-service.ts; transport.send() must be called AFTER db.transaction() commits (not inside), else nested connection deadlock; MessageEnvelope.createdAt is Date not string; inArray needs tuple cast `as unknown as [T, ...T[]]` for enum columns.
- [lib/db tsc rebuild](lib-db-rebuild.md) — after schema changes, run `pnpm --filter @workspace/db exec tsc --build` to regenerate dist/.d.ts; tests/api-server reference compiled output and see stale types until rebuilt.
- [Test fixture required fields](test-fixture-required-fields.md) — users.name NOT NULL (no orgId/role columns); takte requires taktBezeichnung+zone+gewerk NOT NULL; projects requires agOrgId NOT NULL; users table has no orgId column.
- [Sprint 7 deadline system](sprint7-deadline-system.md) — full deadline/reminder system: 5 new takt_requests columns, takt_request_reminders table, evaluateTaktRequestDeadlines() two-phase (REMINDER_ELIGIBLE + GU_DECISION_ELIGIBLE), hub_messages gets correlationId + 2 new enum values, DeadlineConfig field names are firstReminderHoursBeforeDue/secondReminderHoursBeforeDue/overdueReminderHoursAfterDue.
- [project_contractors schema v2](project-contractors-v2.md) — extended with id PK (UUID), trade, work_package_reference, assignment_status enum, validFrom, validTo, createdByUserId; unique index (project_id, an_org_id, COALESCE(trade,''), COALESCE(work_package_reference,'')); only ACTIVE assignments valid for new TaktRequests.
- [takt_requests has no projectId](takt-requests-no-project-id.md) — takt_requests has no project_id column; join takt_requests → takte to get projectId for project-level queries.
- [AN-App TaktRequest migration](an-app-taktrequest-migration.md) — AG "Takt vergeben" uses createTaktRequestWithSnapshot+sendTaktRequest (2 steps); cancel = CLOSE_WITHOUT_AGREEMENT GU decision (only after NU responds); AN detail at /takt-requests/:id; dashboard backend uses taktRequestsTable not delegationsTable; responseRequiredBy is PgTimestamp → pass Date objects not strings to drizzle gte/lte.
- [TaktRequest audit trail](takt-audit-trail.md) — takt_request_audit_events table + writeAuditEvent service; DETAILS_RETRIEVED written on first NU access only; audit-trail endpoint at GET /api/takt-requests/:id/audit-trail; EDC readiness in docs/dataspace-readiness.md.
- [Project endpoint security](project-security.md) — requireProjectOwner pattern (routes/projects.ts); soft-delete requirement for projects+contractors; internal endpoint token auth at /internal (not /api/internal); Vitest env var setup pitfall.
- [Response unification & hash idempotency](response-unification.md) — both create routes use createTaktRequestWithSnapshot(); no lazy snapshot in /send; processNuResponse() in nu-response-service.ts owns all response logic; responsePayloadHash (SHA-256, canonical sorted-key JSON) on takt_responses; legacy /response translates flat to canonical; idempotency: same hash→200, diff hash→409; status+response in single tx; UNKNOWN (not DELIVERED) when outbox row missing.
- [Dataspace policy gate](dataspace-policy-gate.md) — details endpoint gates NU on dataPublicationId+policyAcceptedAt; test tokens need orgType; policyAcceptedAt (not acceptedAt) is the correct column name; S2/S3/S4 cleanup must delete TaktRequest BEFORE publication due to FK.
- [DTC integration](dtc-integration.md) — 4 DTC v2 class URIs mapped to AN resource types; two-path availability check (DTC requirements table vs legacy snapshot); resource_bookings.resource_id now nullable; generated OpenAPI enum missing CREW/OTHER (cast as any).
- [Working-day scheduling](working-day-scheduling.md) — project_calendars table (psql migration done); takte.duration_days nullable numeric; GET/PUT /api/projects/:id/calendar routes; computePlannedEnd utility in api-server/src/lib/working-days.ts; API client hooks appended at end of generated api.ts; dep creation now asks reschedule=yes/no via AlertDialog (skipReschedule query param on dep POST); TaktDependencyCreateResult.moved is Takt[] not RescheduledTakt[].
- [Canonical schema rename and raw SQL](canonical-rename-raw-sql.md) — adapters cover ORM calls only; migrate direct production and test SQL when physical tables are renamed.
- [DTC resource semantics](dtc-resource-semantics.md) — type bookings are nullable-resource reservations with positive quantity; DTC class mapping must not invent an OTHER fallback.
- [Booking requirement metadata](booking-requirement-metadata.md) — auto-bookings retain original quantity/utilization and shift requirement sub-periods with the accepted Takt window.
- [API Zod codegen exports](api-zod-codegen-exports.md) — keep api-zod exports explicit because broad generated type re-exports can create duplicate symbols.
- [API Zod codegen overwrite](api-zod-codegen-export.md) — Orval can overwrite barrel-export fixes during codegen; the durable fix belongs in generator flow, not generated output.
- [Coordination regression contracts](coordination-regression-contracts.md) — regression fixtures must use canonical Leistungsanfrage states, idempotent response codes, linked resource types, and current UI props.
- [Concurrent agent edits](concurrent-agent-edits.md) — serialize changes to shared backend services; parallel writers can overwrite each other’s working copy.
- [Coordination and booking sources](coordination-and-booking-sources.md) — derive objective next actions centrally; persist confirmed bookings only from unaggregated booking requirements.
- [Timeline and dependency authority](timeline-and-dependency-authority.md) — initial agreement comes only from a real decision; service-request dependencies map to canonical Leistung dependencies.
- [Invitation delivery retry](invitation-delivery-retry.md) — project invitation retries must use the persisted outbox envelope and original message ID, never recreate membership or response business rows.
- [Physical database boundaries](physical-database-boundaries.md) — AG, AN, and Hub require distinct URLs, role-scoped access, and connector-mediated cross-domain exchange.
- [Atomic invitation delivery](atomic-invitation-delivery.md) — pre-created transactional outbox rows must allow the first delivery retry from PENDING.
- [AN API namespace routing](an-api-namespace-routes.md) — AN fetch rewrites `/api/*` to `/api/an/*`; AN-facing handlers need an AN-subrouter mount.
- [Wouter test router base](wouter-test-router-base.md) — use an empty test Router base for root-relative href assertions; slash bases can produce double-slash links.
- [Shared test DB schema drift](shared-test-db-schema-drift.md) — project_memberships may lack additive ORM columns until the shared database is migrated.
